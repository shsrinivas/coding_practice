/*
Name: Shreya Srinivas
Date:
Description:
Sample Input:
Sample Output:
*/

#include<stdio.h>
#include<ctype.h>

int main()
{
#if 0
	char ch;
	for(;(ch=getchar())!=EOF;)
	{
		putchar(toupper(ch));
	}
	puts("EOF received!\n");
#endif
#if 0
	char ch;
	for(;(ch=getc(stdin))!=EOF;)
	{
		putc(toupper(ch),stdout);
	}
	puts("EOF received!\n");
#endif
#if 0
	char str[10];
	puts("Enter string:");
	fgets(str);
	puts(str);
#endif
    #define num 3
	printf("Emertxe"+num);


}


